{
  "name": "agent-catalogue",
  "version": "1.0.0",
  "description": "Agent-first product catalogue with schema.org JSON-LD, llms.txt, Meta CAPI, and AI media generation. Zero dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate-media": "node scripts/generate-media.js",
    "generate-media:videos": "node scripts/generate-media.js --videos"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
